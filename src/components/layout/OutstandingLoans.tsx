import React, { useState } from 'react'
import { Image, Text, Box, Card, CardBody, Flex, useColorModeValue, Button, Input, Icon, TableContainer, Table } from '@chakra-ui/react'

export function OutstandingLoans() {
  return (
    <div>
      <TableContainer>
        <Table variant="simple"></Table>
      </TableContainer>
    </div>
  )
}
